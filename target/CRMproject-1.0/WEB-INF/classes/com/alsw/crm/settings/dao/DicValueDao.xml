<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alsw.crm.settings.dao.DicValueDao">

    <select id="getValueByCode" resultType="DicValue">
        select * from tbl_dic_value where typeCode = #{code}
    </select>

</mapper>
